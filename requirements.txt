# ─────────────────────────────────────────────────────
#  Diabetes Prediction System — requirements.txt
#  Python 3.10 compatible (tested range: 3.10.x)
# ─────────────────────────────────────────────────────

# ── Web Framework ──────────────────────────────────────
fastapi==0.111.1
uvicorn[standard]==0.30.6
python-multipart==0.0.9          # required for form/login endpoints

# ── Database ────────────────────────────────────────────
sqlalchemy==2.0.31
psycopg2-binary
cryptography==42.0.8             # pymysql SSL + python-jose dependency
alembic==1.13.2                  # schema migrations

# ── Auth & Security ─────────────────────────────────────
python-jose[cryptography]==3.3.0 # JWT create / verify
passlib[bcrypt]==1.7.4           # bcrypt password hashing
bcrypt==4.0.1                    # pin to 4.x — passlib breaks on bcrypt 4.1+
python-dotenv==1.0.1             # load .env variables
pydantic-settings==2.3.4         # typed Settings from env
pydantic[email]==2.8.2           # request/response schemas + email validation

# ── ML Core ─────────────────────────────────────────────
scikit-learn==1.5.1
joblib==1.4.2
numpy==1.26.4                    # last version with full Py3.10 wheels
pandas==2.2.2

# ── PDF Report & Charts ─────────────────────────────────
reportlab==4.2.2                 # PDF generation
matplotlib              # chart PNGs embedded in PDF
Pillow==10.4.0                   # required by reportlab for image handling

# ── Dev / Testing ───────────────────────────────────────
# Remove these in production or move to requirements-dev.txt
httpx==0.27.0                    # AsyncClient for FastAPI TestClient
pytest==8.3.2
pytest-asyncio==0.23.8